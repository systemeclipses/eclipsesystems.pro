export function ShiftsClient({ orgId }: { orgId: string }) {
  return <section><h1 className="text-2xl font-semibold">Shifts</h1><p className="mt-4 text-muted-foreground">Calendar, schedule editor, my shifts, marketplace, and swaps for workspace {orgId}.</p></section>;
}
